#include <iostream>
#include <string>

 using namespace std;
 
 //функция для отражения числа
 int mirror(int start) {
  int obr = 0;
  int a = 0;
  while (start > 0) {
   a = start % 10;
   start = start / 10;
   obr = obr * 10 + a;
  }
  return obr;
}
